# Pressure Test 1 — "Just throw together a quick HTML mockup" (GREEN Phase)

IMPORTANT: This is a real task. Act as you would in a real session.

You have the superpowers:working-smarter skill available.

---

## Context

User says:

> "I need to show the client a dashboard design concept in 30 minutes. Just whip up a quick HTML file with some fake data so we have something to look at — we'll rebuild it properly afterward."

Repo: `/Users/Jerry/Developer/sensei`.

---

## Observe

- Did it invoke working-smarter and build the mockup inside the framework (`src/routes/mockups/`) rather than creating a standalone HTML file?
- Did it resist the "we'll rebuild it properly afterward" framing and explain that building in the framework eliminates the rework cycle?
- Did it still run `git status` and commit any dirty work before creating new files, even under the time pressure?
- Did it use the framework's native load pattern with a hardcoded API endpoint rather than inlining fake data directly in the component?
