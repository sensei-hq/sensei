# Project Context — sensei

> Auto-generated by sensei. Update as needed.

## Gate Check (REQUIRED — do this at session start)

If the session-start hook injected `<sensei-session>`, mindsets and rules are already loaded — skip to Session Protocol.

**If `<sensei-session>` is NOT present**, read these files NOW before doing anything else:
1. Read `.sensei/mindsets/*.md` — 7 mindsets (3 core + 4 specialist), each with questions to ask
2. Read `.sensei/rules.md` — project rules, patterns, quality policy
3. Read `.sensei/personas/*.md` — who we're building for, each with questions to ask

**Core discipline:** Apply mindsets in sequence (Analyst → Developer → BAT) on every task. This is non-negotiable. If you catch yourself writing code without having done an Analyst pass, stop and do it.

## Session Protocol

**Start of every task:**
```
get_session_context(task_description="<brief description>")
```

**During work:** use `search()`, `get_callers()`, `get_callees()` over grep/file reading.

**End of every task:**
```
checkpoint(outcome="completed|blocked|partial", summary="<what was done>")
```

## Stack
- rust (daemon: crates/senseid, MCP: crates/sensei-mcp, CLI: crates/sensei-cli)
- typescript/svelte (desktop: apps/desktop)
- bun (package manager)

## Code Quality Policy

**Zero Errors Policy:** Run the test suite before AND after every change. Zero means zero.

```
Checkpoint 1 (before coding): cargo test --manifest-path crates/senseid/Cargo.toml
Checkpoint 2 (after coding):  cargo test --manifest-path crates/senseid/Cargo.toml
```
