# Session: 2026-04-23 — Database Architecture Redesign

> Session memory for continuity. What was done, gap analysis findings, and granular issues for next sessions.

## What was done

### Database schema (5 schemas, 39 tables)

- `gateway` (6) — LLM routing: providers, models, routers, models_in_router, fallback_chains, fallback_chain_models
- `sensei` (18) — structural: folders_to_watch, folders, projects, tags, nodes, edges, libraries, library_pages, referenced_libraries, extensions, services, config, workflow_state, scan_state, index_errors, memory_items, benchmark_reports, benchmark_runs
- `inference` (10) — derived: detected_patterns, communities, hyperedges, hyperedge_members, drift_items, recommendations, reasoning_traces, preferences, insights, insight_batches
- `activity` (4) — tracking: sessions, events (unified 12 types), task_sessions, snapshots
- `history` (1) — audit: past_extensions + historize_extensions() trigger

Key refactors: generic nodes+edges, embeddings on nodes, unified events, all repo_id→folder_id, all check→enum (31 enums), ~30 tables deleted.

### Documentation

- README rewritten (FTR-centric)
- 30 ideas, 9 user journeys + 3 system pipelines, database-schema.md
- Mockups mapped to journeys with design briefs for 23 missing screens

### Commits

26 commits on develop branch.

---

## Gap analysis findings

### 1. Broken references (fixed end of session)

- ~~`user_teams` view referencing deleted `core.*` tables~~ → deleted
- ~~`sqlite-schema.md` still present~~ → deleted
- ~~Stale seed data (accounts.jsonl, teams.jsonl, repos.jsonl)~~ → deleted
- ~~`import_teams.ddl` procedure referencing deleted staging table~~ → deleted
- ~~Schema doc said 42 tables, actual is 39~~ → fixed

### 2. Missing system journeys

| Gap | Description |
|-----|-------------|
| Collective intelligence sync pipeline | Idea 29 — how insights are batched, anonymized, shared. No system journey. |
| Pattern detection pipeline | Idea 15 — how patterns flow through suggested→gap→rule. No system journey. |

### 3. Missing design docs

| Gap | Description |
|-----|-------------|
| Bootstrap design | J1 has no dedicated design doc (only journey + idea 26) |
| Memory data model | J9 / idea 30 — data model is TBD, `memory_items` + `preferences` pending replacement |

### 4. Mockup coverage: 32%

11 of 34 screens have mockups. 23 need new mockups. Design briefs exist in journey docs.

**Mockup file → journey mapping:**

| Mockup file | Journey | Screens |
|-------------|---------|---------|
| `mockups/lib/setup-wizard.jsx` | J2 | Welcome, assistants, folders, scan, projects, libraries, MCP registry, enter |
| `mockups/lib/setup-data.js` | J2 | Mock data for setup wizard |
| `mockups/lib/observatory.jsx` | J3 | Observatory early + mature modes |
| `mockups/lib/navigation.jsx` | J3 | Project navigation (cards, Cmd-K palette, tree browser) |
| `mockups/lib/data.js` | J3, J4 | Session + event mock data |
| `mockups/lib/project-shared.jsx` | J5, J6 | Project overview, graph, patterns, recommendations, action drawer, settings |
| `mockups/lib/project-pages.jsx` | J5 | Project page layout variants |
| `mockups/lib/libraries.jsx` | J5 | Library list/detail, MCP playground (partial) |
| `mockups/lib/project-data.js` | J5 | Project, pattern, recommendation, graph mock data |
| `mockups/lib/tokens.css` | All | Design system tokens |
| `mockups/lib/primitives.jsx` | All | Shared UI primitives |
| `mockups/lib/design-canvas.jsx` | — | Design exploration harness |
| `mockups/lib/direction-*.jsx` (4) | — | Design direction explorations |

### 5. Database tables with no user-facing journey

| Table | Note |
|-------|------|
| `gateway.*` (6) | J7 mentions inference settings but no provider/model/router management screen |
| `hyperedges` + `hyperedge_members` | Code graph may use implicitly but not mentioned in J5 |
| `insight_batches` | Collective sync has no journey or system pipeline |
| `past_extensions` | Extension history visible nowhere in journeys |

### 6. Naming mismatches

- J3 observatory data sources reference `change_impacts` → should be `recommendations` with `verdict`
- `memory_items` and `preferences` pending replacement by contextual memory system

---

## What is pending

### Next session priorities

1. **Memory data model** — design tables for contextual memory (idea 30), replace `memory_items` + `preferences`
2. **Create GitHub issues** — granular TDD-ready issues below
3. **Gateway as independent repo** — extract gateway schema into separate repo/subtree
4. **Missing system journeys** — collective intelligence sync + pattern detection pipelines

---

## Backlog: TDD-suitable issues

Granular, well-defined tasks with clear inputs/outputs. Each can be worked unattended using test-driven development.

### Database layer — `crates/senseid/src/db/`

| # | Issue title | Description | Test approach |
|---|-------------|-------------|---------------|
| 1 | Migrate connection pool to PostgreSQL | Replace rusqlite with sqlx::PgPool | Integration: connect to test DB, health check |
| 2 | Add folders_to_watch CRUD | create, list, update_status, update_excluded | Test: create root, verify scanning→watching lifecycle |
| 3 | Add folders CRUD | upsert, list_by_root, list_by_project, delete_tree | Test: create parent→git hierarchy, verify cascade delete |
| 4 | Add projects CRUD | create, update, list, get | Test: create project, update maturity, verify stack jsonb |
| 5 | Add nodes CRUD | upsert, get_by_folder, get_by_file, get_children, delete_by_folder | Test: create file→class→method tree, query by file_path |
| 6 | Add edges CRUD | insert, get_callers, get_callees, get_by_kind | Test: create nodes + call edges, verify bidirectional lookup |
| 7 | Add sessions CRUD (activity schema) | create, complete, get, list_by_project | Test: create session, complete with ftr=true |
| 8 | Add events insert (activity schema) | insert, get_by_session, get_by_type | Test: insert tool_call + correction events, query by type |
| 9 | Add extensions CRUD + verify historize | create, update, get_history | Test: create skill, update, verify past_extensions snapshot |
| 10 | Add libraries CRUD | upsert_library, add_page, link_to_folder | Test: create library, add pages, query libraries_in_project view |
| 11 | Add detected_patterns CRUD (inference) | upsert, promote, list_by_folder | Test: create suggested, promote to rule, verify lifecycle |
| 12 | Add recommendations CRUD (inference) | create, accept, measure | Test: create, accept (baseline_ftr set), measure (verdict) |
| 13 | Add communities CRUD (inference) | upsert, list_by_folder | Test: create with god_node_ids, verify node_count |
| 14 | Add scan_state CRUD | upsert, get_stale_files, delete_by_folder | Test: insert, check mtime comparison logic |
| 15 | Wrap match_embeddings function | Rust caller for PG function | Test: insert nodes with vectors, call, verify ranking |
| 16 | Wrap rank_bm25 function | Rust caller for PG function | Test: insert nodes, search query, verify ranked results |
| 17 | Wrap rank_bfs function | Rust caller for PG function | Test: insert import edges, query changed files, verify BFS |

### Task queue — `crates/senseid/src/tasks/`

| # | Issue title | Description | Test approach |
|---|-------------|-------------|---------------|
| 18 | Update task queue to folder_id | Task struct uses folder_id, reads from folders | Test: enqueue for folder, dequeue, verify |
| 19 | Update code processor to write nodes | Write file + symbol nodes with parent_id | Test: process Rust file, verify node hierarchy |
| 20 | Update code processor to write edges | Write call + import edges with confidence | Test: process file with calls, verify edge creation |
| 21 | Add community detection post-processor | Leiden on graph, write community_id | Test: small graph, run detection, verify assignments |

### Watcher — `crates/senseid/src/watcher/`

| # | Issue title | Description | Test approach |
|---|-------------|-------------|---------------|
| 22 | Update watcher to read folders_to_watch | Use excluded jsonb for filtering | Test: create root + exclusions, verify skip |
| 23 | Update watcher project mapping to folder_id | Map folder_id→abs_path | Test: create folders, trigger change, verify folder_id |

### API — `crates/senseid/src/api/`

| # | Issue title | Description | Test approach |
|---|-------------|-------------|---------------|
| 24 | Update list endpoint to use folders/repositories view | Return git+subtree folders | Test: create folders, call endpoint, verify shape |
| 25 | Update session endpoints for activity schema | Read activity.sessions + events | Test: create session + events, verify timeline |
| 26 | Add recommendations endpoint | GET /recommendations?project_id | Test: create recommendations, verify urgency sort |

### MCP — `crates/sensei-mcp/`

| # | Issue title | Description | Test approach |
|---|-------------|-------------|---------------|
| 27 | Update search() to query nodes | FTS or name/signature match on nodes | Test: insert nodes, search, verify results |
| 28 | Update get_callers/get_callees to query edges | edges where kind=calls | Test: insert call edges, query, verify |
| 29 | Update get_patterns() for inference schema | Query inference.detected_patterns | Test: insert patterns, verify lifecycle filter |
| 30 | Add report_learning() tool | New MCP tool for two-way memory | Test: call with structured input, verify stored |
| 31 | Update get_session_context() for memory assembly | Multi-source context per checklist | Test: create memories at different scopes, verify markdown |

### Seed data

| # | Issue title | Description | Test approach |
|---|-------------|-------------|---------------|
| 32 | Create gateway seed data loader | Run all 6 import procedures | Test: run against empty DB, verify all tables populated |
| 33 | Add import procedure idempotency tests | Verify upsert + freshness gate | Test: load, modify, re-load, verify only changed rows |
