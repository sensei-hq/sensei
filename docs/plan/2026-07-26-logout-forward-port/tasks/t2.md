# t2 — Verify the dojo builds clean

## What
Run the dojo zero-errors gate after t1's edit.

## How
`cd dojo && bun run check`  (svelte-check: types + a11y).

## Verify (observable)
- Output = `0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`.

## Deps
t1 (the edit must land first).
