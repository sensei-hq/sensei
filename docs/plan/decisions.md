# Decisions log — considered · adopted · discarded · deferred

> When a doc is superseded and archived, the *ideas* worth keeping land here so
> nothing useful is lost and nothing already-rejected gets re-proposed. This is
> the memory of **why the shape is the shape**. Additive — append, don't rewrite
> history. Pairs with [the plan](README.md)
> (what's left to build) and [`backlog.md`](../backlog.md) (the issue index).

## Adopted — concepts carried forward (salvaged before archival)

| Concept | Where it lives now |
|---|---|
| The **four-segment journey** (Bootstrap · First-run+Prefs · Observatory · Project) | [vision.md](../vision.md), [objectives.md](../objectives.md) |
| **Value before setup** — projects first, not a wizard | vision theme 1; realised as first-run-scan + Preferences |
| The **module lifecycles** (the loops inside the daily app) | objectives O5 |
| **FTR** as the single north-star | vision.md |
| **Adapter-IR** + language-adapter split; task hierarchy + barriers; compression L0–L3 | [architecture/daemon.md](../architecture/daemon.md) |
| **Single binary, single DB, port 7744** | architecture/data.md + daemon.md |
| The **retrospective-loop** framing (capture→graph→analyze→learn→deliver→measure) | vision.md core loop |
| Dōjō **priority-ladders / specificity-wins / pull-never-push / preview-always** | [architecture/dojo.md](../architecture/dojo.md), objectives DJ* |

## Adopted — decisions with rejected alternatives (ADRs)

The *why*, so a rejected shape isn't re-proposed. Salvaged from the old `design/decisions/` ADRs + `ideas/`.

| Decision | Rejected alternatives (why) |
|---|---|
| **One repo = one project = one owner** (#101) | promoting every walked subfolder/crate to a project — double-indexes and gives folders their own code nodes. Crates/packages are **structural members with a role**; only a genuine nested `.git` subtree is a separate owner. Enforced at classification + self-healed by `dedup_structural_folder_nodes`. |
| **Postgres + pgvector as the single store** (:7744) | SQLite+Kuzu (no cross-store txns, N+1 on JSON-array joins, Kuzu unmaintained/embedded-C++ risk, no vectors); FalkorDB/Chroma/MemPalace (need a server process — wrong for a zero-server desktop); in-memory queue (lost task state on restart). Postgres unifies relational + graph + vectors + a durable queue with concurrent app/MCP/CLI connections. |
| **Code is a graph → relational tables + recursive CTEs** | a flat vector index or regex extraction — destroys the call/import/type graph; cosine surfaces files that *mention* similar words but can't answer "who calls X". Recursive CTEs cover 90%+ (1–2 hop); Apache **AGE** can layer on the *same* tables later with no migration. |
| **Adapter-IR = three node types** (`IRDoc`/`IRModule`/`IRClass`), all `Option<>` | one unified symbol type — structure differs per kind. `Option` everywhere so missing frontmatter/annotations degrade gracefully; per-file parse is worker-parallel, edge/parent resolution is a separate batch phase. |
| **Preferences merge INTO memory** | a separate `inference.preferences` table — memories are part of the graph, not a silo. The table was killed. |
| **Agent-agnostic MCP surface** | per-assistant tool variants — per-assistant differences live in a coordinator *adapter*, never in the tool contracts. The AI only ever reaches the daemon via MCP. |
| **Vertical feature slices** (daemon→MCP→command in one issue, innermost-first, fully tested before the next layer) | per-layer issues + stubs — they cause rework cycles (D18). |
| **Daemon owns the autonomous-run tick** ([relay-engine §5](relay-engine.md#5-the-run-engine-daemon-owned)) | a **session-coupled tick** (Claude Code cron/ScheduleWakeup) — died when the session ended and on an unhandled limit (the 5-day run stopped twice). The daemon is long-lived, holds durable run-state, and handles limit-with-reset-time (pause→auto-resume) + crash recovery. |
| **Autonomous run = progress over asking** ([relay-engine](relay-engine.md#autonomous-execution--progress-over-asking)) | **3-try-then-park-and-stop** (halts the thread). Unknowns → reasoned default + non-blocking **advisory flag** (async review); hard-block only for irreversible/safety/out-of-scope. A pre-flight **depth bar** fixes unknowns upstream. |
| **Hybrid model routing to cut token burn** ([relay-engine §6.5](relay-engine.md#65-utilization--keep-the-platform-busy)) | all-cloud-Opus for everything — burns tokens + hits rate limits fast. Route by task: Opus = reviews + hard reasoning; Sonnet/local-Qwen = coding; local gemma4 = lighter reasoning; small/mechanical → local ollama. Reviewer agents stay on the strong model. |
| **The synthetic example is a column on `sensei.memories` (`generalised_example`), not a row in `sensei.memory_examples`** (2026-08-30) | reusing the existing `sensei.memory_examples` table — it is the semantic opposite. Its `node_id text **not null**` references `hierarchy_nodes.id` and its table comment reads *"Code examples grounding a memory in **real code**"*. A synthetic example is defined by pointing at NO real code, so it could only live there by fabricating or nulling a not-null FK column and making that table's own comment false. The generalisation also rewrites rule and example together (an example must not outlive the rule it illustrates), which is a single-row update, not a child-table lifecycle. The `generalised_*` prefix is load-bearing: it names the lane that may leave the machine, next to `content`, which may not. |

## Discarded — considered and rejected (do not re-propose)

| Idea | Why rejected | Superseded by |
|---|---|---|
| **10-step linear setup wizard** (old `ideas/02-setup.md`) | A first-run gauntlet violates *value before setup*; users abandoned it | first-run-scan (value immediately) + a searchable **Preferences** surface |
| **dev/prod split** (two binaries, port 7745) | Needless complexity; one machine, one dataset | **single mode** — one binary, one DB, :7744 |
| **Gateway as an in-tree crate** (`crates/gateway/`) | Couldn't release to crates.io independently | **`gateway-embedded`** git dep (sibling repo `sensei-hq/gateway`) |
| **`hive-mind` / `sensei-hive` naming** | Ambiguous; drifted from the product concept | **Dōjō / `sensei-dojo`** (`crates/dojo-mind`) — terminology migration in progress |
| **Push-based federation** | Leaks control + confidentiality | **pull, never push** + preview-always (theme 5) |
| **Global collective as the default share lane** | Org/client work must not hit the public commons | the **Dōjō membership** boundary is the default; Collective is explicit opt-in |
| **`client-lead` role name** | Longer than needed; the engagement (not the person) is the "client" | role renamed to **`lead`**; it still guards *client engagements* |

## Deferred — good, not now (parked with a trigger)

| Idea | Why deferred | Revisit when |
|---|---|---|
| **User-facing learnings** ("you tend to give sparse instructions for schema changes…") | The pair-both-ways signal needs the loop closed first | after Phase 1 (FTR loop closes) |
| **Assistant proactive clarification** ("I need the migration policy before I can answer") | v2 behaviour; needs the signal + a prompting contract | [pipeline/clarification-prompting](../spec/pipeline/clarification-prompting.md) — post-Phase 2 |
| **Benchmarks** (runner + corpus) | No runner/TaskKind yet; not on the FTR path | when model-effectiveness needs a controlled corpus |
| **Testability** (test-runner adapter → `test_pass_rate`) | No test-runner integration; quality signal optional | when a quality dimension is prioritised |
| **Diagnostic sessions/traces + issue export** (#39) | Larger new-schema + cross-cutting capture effort; only flat `public.logs` today | when support/debug UX is prioritised |
| **`generalised_title` — generalise the artifact title too** (2026-08-30) | The share path sends `memories.title` VERBATIM (`batch_share_items`); there is no rewrite behind it, only the deterministic identifier strip, which passes ordinary prose. For `origin='learned'` the title is a truncated slice of the user's own prompt (`tasks/handlers/generate.rs`), so a prompt sentence can leave the machine next to a carefully generalised body. Fixing it properly is its own slice: a new column, a prompt/parser change, a migration across the three DBs, and a decision about what to do with the ~all existing rows whose title would then be un-generalised (hold them, or ship title-less). Out of scope for the example slice, which deliberately did not touch the confidentiality gate. | next Dōjō-sharing slice — before the collective lane is opened to real users |
| **`HeldNotGeneralised` should write an outbox row and be reported once per memory** (2026-08-30) | The guard sits inside the per-destination loop and returns before `build_artifact_for_target`, so one un-generalised memory routed to two memberships reports `held: 2` with `membership_id: None` on both, and — unlike `HeldResidualRisk` — writes no `sensei.dojo_outbox` row, leaving the hold with no durable trace for a later sweep. Harmless today (it refuses correctly and names itself), but the roll-up over-counts and nothing can reconcile it later. Changing the loop shape touches the guard placement agreed in c9fdd788. | when the outbox gets a sweep/retry surface |
| **Enforce "synthetic" mechanically — compare `generalised_example` against `content`** (2026-08-30) | Today "synthetic" is a *generation contract* carried by the prompt (`GENERALISE_REPLY_CONTRACT`) and nothing verifies it; a small local model that parrots its input can land real function names in the example, and the deterministic strip only catches KNOWN identifiers. A similarity/overlap check is the real fix but needs a threshold that does not reject legitimately-similar illustrations, and it belongs next to the confidentiality gate — parked by user decision this cycle. | when the gate is reopened; pair it with `generalised_title` |
| **Image-gen as seed** (#77) · **embedded in CI release binaries** (#78) | Need `model_capability=image` / cross-platform native llama.cpp sign-off | gateway/seed hardening pass |
| **Dōjō live activation** | External-blocked (needs a remote server + SaaS-infra decision) | Phase 4 |
| ~~ACP + control-plane / relay~~ **PROMOTED to an active vision 2026-07-14** | adopt Apache-2.0 **ACP**, not Zed's GPL agent crate; agents on your hardware, filtered status only, daemon outbound-only | now specced: [requirements](../vision.md#the-relay--long-runs-supervised-from-anywhere) R1–R8 + [architecture/relay](../architecture/relay.md) + a plan phase. Still future to *build* |
| **Response cache** (semantic cache of notable LLM outputs — TTL/pin/secret-scrub, ≤3 session-start hints) | distinct from the memory pipeline; never built | when prompt/response reuse becomes a cost lever |
| **Telemetry transactional-outbox** (durable event write-buffer + backoff/eviction + queue-depth health) | accepted direct-write today | when "no events lost during downtime" is prioritised |
| **Data-source connectors** (Confluence/Jira/Notion/Figma as graph-node producers — "the graph doesn't care whether a node came from git or Confluence") | repo-only today | when Solution scope needs non-repo members |
| **Cursor as 2nd coordinator** + generic capture fallback ladder (MCP/file-watch/poll/OTLP for tools without hooks) | Zed shipped first; Cursor = largest user base | when a 2nd coordinator is prioritised |
| **Personal-Dōjō membership seed** (Relay beta shortcut — hand-seed a `personal/<you>` membership + Keychain device token instead of the unbuilt join flow; [relay-engine D4](relay-engine.md#3-principles--locked-decisions-2026-07-16)) | dogfood needs auth *now*; the real join/claim flow is Phase-4 scope | **Phase 4** replaces it with the real join flow. **Guard: the seed path is dev/beta-only and must not become a prod auth path** — R0 gates it so a hand-seeded membership/token can't leak through |
| **Relay across assistants — an orchestrator + adapter layer** behind `crates/senseid/src/assistants/trait_def.rs`, normalizing every assistant to one `Execution → Segment → Item` relay view ([relay-engine → Multi-assistant](relay-engine.md#7-multi-assistant--the-adapter-layer)). Extends the capture-ladder + Capability-Registry rows above. | the beta is Claude-hook-only (no orchestrator); other assistants lack Claude's hooks so a uniform live feed needs adapters (hooks / ACP / fallback ladder) + a control surface (start·pause·gate·nudge·observe) | after the Claude-hook beta proves the loop (post-R2); **never builds the agent runtime** — supervises + normalizes existing assistants |
| **Relay daemon-auth plane B — signed payloads** (daemon signs each request with its device private key; Worker verifies via `dojo.memberships.device_key`) instead of the beta's plane **A** (bearer `dojo.memberships.device_token_hash`, sha256) | beta uses A — matches the current `dojo/client.rs` bearer, fastest to a working round-trip, revocable. B is stronger (no shared secret ever crosses the wire; matches `device_key`'s stated purpose) | **before real/prod device tokens** (security-hardening pass) — needs daemon-side signing (Ed25519) in `dojo/client.rs`. Confirmed A for beta 2026-07-16. |
| **Relay device-token store → a `dojo.membership_device_tokens` table** (many tokens per membership — multi-device + rotation + per-token revocation; mirrors `dojo.api_keys`) instead of the beta's single `dojo.memberships.device_token_hash` column | beta = one daemon/device per membership (matches `DojoClient::for_membership`'s single `credential_ref`); the column is simplest | when multi-device-per-Dōjō or token rotation is needed — a clean additive migration. Confirmed column (A) for beta 2026-07-16 ("may expand to B later"). |
| **Supabase deploy step: expose + grant the `dojo`/`sensei` schemas** — PostgREST needs the schemas in `[api] schemas` AND `grant usage on schema dojo + all on tables/sequences to service_role` (+ usage to authenticated/anon), or every `/v1` route 500s ("permission denied for schema dojo"). Codified in the local round-trip seed ([`scripts/relay-roundtrip.sh`](relay-roundtrip.sh)); deliberately NOT in the dbd tree (dbd's parser rejects anonymous `DO` blocks, and Supabase-role grants are non-portable — they'd fail on the daemon's plain Postgres). | the `dojo` schema is created by the dbd superuser, ungranted; Supabase roles only exist on Supabase | a one-time per-Dōjō-Supabase step — local via the harness seed; **prod = a documented deploy step** (P6/Dōjō-go-live). |
| **Relay RLS** (`dojo.relay_*` row-level security — a user owns their rows) | the beta Worker mediates ALL access via `service_role` (bypasses RLS) and the phone never touches Supabase directly, so RLS isn't in the beta threat model | when the phone/console reads Supabase directly, or multi-user tenants need row isolation — a hardening pass alongside plane-B (moved out of P1 2026-07-16). |
| **Capability Registry** (per-ACP data-availability Real/Workaround/Unavailable + `discard_when` upstream-issue + workaround lifecycle) | keeps workaround code inert until upstream lands, then auto-cleaned | when cross-ACP graceful degradation is built |
| ~~`llm-spec/` → `spec/` rename~~ **DONE 2026-07-14** | was high-churn (run-state/cron driver + gate agents + memory) | completed: dir renamed + all referrers fixed (docs, `.claude/agents`, code comments) |
| **Daemon bind `127.0.0.1` (or local-secret/Unix-socket for `/hook/*` + `/api/*`)** — daemon binds `0.0.0.0` with no auth on any route (`api/server.rs`); P2's `/hook/gate` and **P3's `POST /api/runs`** (unauth run creation) + `/api/runs/*` are control-plane surfaces on that open bind (a LAN-adjacent host can POST spurious gates → phone-prompt nuisance/DoS, or create inert run rows; neither can force allow/deny beyond fail-open, and a created run only drives if `SENSEI_RUN_DRIVE` is on). *(P2 security-review #4 + P3 security-review #4 — pre-existing daemon-wide since 2026-05, NOT a P2/P3 regression.)* | daemon exposes only local endpoints today; never validated as LAN-safe | a daemon-wide hardening pass — **must precede any non-loopback deployment AND any default-on `SENSEI_RUN_DRIVE`** (P3 adds process-spawn + a safety-gate to the exposed surface); own issue |
| **Relay hard-block classifier — deterministic FLOOR + gemma4 backstop (SHIPPED `0dbfef45`)** — `classify_hard_block` (`dojo/gate.rs`) is literal rule matching. P3 security-review #1 confirmed obfuscation bypasses (arbitrary `base64`/hex/env indirection, novel subshells) and #3 a coverage gap (`Task` subagent prompts). **Hardened in the P3 security pass** (`04618bc5`): `indirect-run` category (`curl…\|sh`, `eval`, `base64 -d`), fixed the `rm -rf target/../..` traversal bypass, `WebFetch`(money)+`NotebookEdit`(secret) coverage. **Then the gemma4 second opinion SHIPPED** (`0dbfef45`): rule-first, model-second — when the deterministic verdict is `None` AND a command carries an obfuscation marker, `hook_gate` asks gemma4 (reasoning chain, 8s timeout, fail-open) for a `{dangerous,category,reason}` verdict; a `dangerous:true` raises the gate. `classify_hard_block` stays PURE (the model layer is the handler's). | a deterministic matcher can't be complete; rule-first+model-second is the standard safety posture (never model-only — the deterministic floor stands alone if gemma4 is down) | **RESIDUAL now only fully-novel obfuscation that BOTH the rules miss AND gemma4 misjudges** — acceptable for `SENSEI_RUN_DRIVE` activation. `Task` subagent calls are separately gated (a subagent's own tool calls re-fire `PreToolUse`). Further hardening (more markers / a dedicated classify chain) is incremental, not a blocker. |
| **Worker-side zero-knowledge defense-in-depth** — the Worker persists `title`/`payload`/`summary`/`detail` verbatim; zero-knowledge is enforced ONLY daemon-side. A daemon bug / rogue device-token could push code/diffs through. *(P2 security-review #3.)* | matches the architecture (Worker = dumb relay, daemon = credential-bearing filter); daemon-side filter + tests cover the happy path | when belt-and-suspenders is wanted — a size cap + diff-marker (`--- a/`,`@@`) reject at the Worker |
| **Generic Worker 500s** — relay routes return raw PostgREST `error.message` (leaks table/constraint names) to the caller. *(P2 security-review #2 — pre-existing pattern, also in engagements route.)* | pre-existing house pattern across `/v1` routes | when hardening the `/v1` surface — log server-side, return `'internal error'`; fix all `/v1` routes together |
| **Relay segment-publish per-run membership scoping** — `publish_relay_segments` fans a run's outline to EVERY enabled membership; a single-membership beta is fine, multi-membership would cross-post a project's outline to all Dōjōs. *(P2 security-review #5.)* | beta = one membership | the multi-membership phase — scope publish to the membership(s) bound to the run's project (`dojo.projects.dojo_id`/`org_slugs` precedence) |
| **Relay limit-parse timezone** — a Claude limit message with an explicit IANA zone (e.g. "reset at 3pm (America/New_York)") is parsed against the daemon's `Local` zone, not the stated zone (`run_limits.rs` `next_occurrence_of_local_time`). Wrong instant if the daemon's zone ≠ the account's (UTC container, laptop zone ≠ billing zone). *(P3.4 review, conf 80.)* | self-heals — a mis-timed resume just re-hits the limit + re-pauses on the 30-min backoff; `Local` is correct for the common laptop-daemon case; a precise fix needs a `chrono-tz` dep | when the daemon runs in a zone ≠ the account's, or precise resume matters — add `chrono-tz` + parse the `(Zone/City)` suffix |
| **Relay daemon-client transient retry** — `DojoClient::relay_post`/`poll_inbox` do single-shot requests; one transient failure fails a whole gate flow (publish→raise→poll). The P1 harness works around this with a `req` retry helper; the daemon has none. Surfaced dogfooding B locally: the vite dev Worker drops POST bodies on cold routes → 400/404 → fail-open. A real CF Worker is precompiled so prod is unaffected, but a bounded retry (idempotent publishes) is reasonable resilience + makes local dev demos reliable. | prod = precompiled Worker (no body-drop); single-shot is fine there | when hardening relay for flaky networks, or to run a reliable LOCAL gate demo before cloud deploy |
| **Console-wide: migrate hand-rolled buttons → `@rokkit/ui` `<Button>`** — the dojo console hand-rolls every button as `<button class="bg-ink text-on-primary …">` (triage, admin, etc.), but `@unocss/reset` preflight sets `[type='button']{background:transparent}` (specificity 0,1,1) which out-specifies the `.bg-ink` utility (0,1,0) → **primary buttons render transparent (invisible) on the dark theme, console-wide**. Relay was migrated to `<Button>` (proven to render correctly — theme CSS already loaded); the rest of the console still hand-rolls. *(Found dogfooding the relay UI in a real browser.)* | pre-existing house pattern; only surfaced now that the relay UI was viewed live on the dark theme | a console-wide UI cleanup — replace hand-rolled buttons with `@rokkit/ui` `<Button>` (design-system source of truth), NOT `!bg-*` important hacks. Fix triage's action button first (most visible). |
| **Relay fail-open warns are invisible** — `hook_gate` (+ relay publish/poll) `tracing::warn!` fail-open reasons aren't bridged to `sensei_logger`/`public.logs` (only explicit `log_event` calls are), and the daemon's default `fmt::init()` filter drops WARN unless `RUST_LOG` is set — so a gate silently failing open leaves NO durable trace. Violates no-silent-errors for a security-relevant path. *(Found dogfooding B — debugged blind until restarted with `RUST_LOG`.)* | tracing macros and the structured logger are separate paths today | bridge api-layer `tracing::warn!/error!` into `public.logs` (or log_event the fail-open reasons) so gate outcomes are auditable |
| **Relay `respond_gate` MCP/local reply channel** — P3.8 shipped `start_run` + `run_status` MCP tools, but **deferred `respond_gate`**: today a raised gate is answered ONLY by the phone via the Dōjō Worker inbox (`hook_gate` → `await_reply` polls the inbox). A local desktop/MCP `respond_gate` needs its own reply path the await loop also checks (an in-daemon reply store, or the daemon posting the reply into the Dōjō inbox as the human). *(P3.8 scoping.)* | `start_run`/`run_status` are clean daemon-HTTP proxies; `respond_gate` touches the gate-await internals — its own small design, better done deliberately than half-built | when the desktop app / MCP needs to answer a gate without the phone — add a local reply channel to `await_reply` (or an app→daemon→dojo-inbox post), then the `respond_gate` MCP tool |

| **Relay `push_subscriptions` no unique (user_id, endpoint)** — the P4.3 subscribe route does select-then-insert/update rather than a DB upsert; a concurrent re-subscribe (two tabs) could dup a row for one endpoint → duplicate pushes. *(P4.3 review, low.)* | scoped to the caller's own rows (not cross-user); rare; a jsonb-expression unique index + `onConflict` is fiddly | P4.4 send dedups by endpoint (cheap mitigation); a unique index on `(user_id, web_push->>'endpoint')` is the durable fix when convenient |

| **Relay push-subscribe endpoint host allowlist** — `POST relay/push/subscribe` stores the browser-provided Web Push `endpoint`, which the Worker later `fetch()`es (P4.4) — an SSRF surface. P4 added an `https://`-only check (blocks http/internal targets); a push-service **host** allowlist (fcm.googleapis.com / *.push.apple.com / mozilla / windows) is NOT enforced. *(P4 security-review #3, low.)* | self-scoped (a user can only push against their OWN subscriptions), CF Workers can't reach RFC1918/loopback, and an incomplete host allowlist would reject legitimate browser endpoints | add the host allowlist when there's a definitive push-service host list, or accept the self-scoped residual |
| **Relay push-send log hygiene** — `relay-push-send.ts` catch-logs may carry a raw error object; if the push lib ever puts the endpoint URL / VAPID JWT in a thrown error's message it'd land in Worker logs. Today only subscription ids are logged (safe). *(P4 security-review #2, low.)* | not exploitable as written (no secret is logged today) | constrain caught errors to `errMsg(e)`-only before logging, so a future lib change can't leak |
| **Relay `GET relay/session` is tenant-wide** — the Worker JWT-plane GET returns ALL of a tenant's relay sessions to any member (separate from, and broader than, the own-rows client-direct RLS path). Fine for the 1-member personal beta; in tension with the "own-rows-only, team=P6" doc. *(P4 security-review data-exposure note — a spec question, not a bypass of the RLS boundary.)* | for a 1-member tenant, tenant-wide == own-rows; tenant-wide is the intended P6 team model | confirm the intended P4-vs-P6 boundary: either scope this GET to `caller.userId` to match the RLS model, or document it as the sanctioned early tenant-visibility surface |

| **Relay browser POST → local wrangler-dev drops the body (spurious 400)** — a browser `fetch` POST to a `/v1/relay/*` route under `wrangler dev --local` intermittently arrives with an empty body → the route sees `{}` → 400 (e.g. `/reply` → "inbox_id is required"), EVEN THOUGH the same request via `curl` to the same warm route returns 200 and writes correctly. Full-cycle verify (2026-07-18): browser Approve → 400 in console, but the row still gets written + the card clears live via realtime. **Route is provably correct (curl 200); prod is a precompiled CF Worker (unaffected).** Same class as the P2 vite-dev body-drop note above. | local-dev runtime body handling, not the route; prod precompiled Worker reads the body fine (curl-equivalent) | environmental — for reliable LOCAL browser dogfooding, hit routes via curl or accept the spurious 400; no code fix (the route + client are correct). Revisit only if it reproduces against a deployed CF Worker |

| **Relay P5.3b — watchdog vs. fallback-coarse `Stalled`** — P5.3's coarse fallback maps Idle→`Stalled` (honest: no `Idle` variant; `Paused`=intentional). Today it's INERT (nothing constructs `FallbackDriver` or writes coarse status to `activity.runs`). When P5.3b wires the live coarse DB-write, a fallback run marked `stalled` would be picked up by the P3 watchdog (`assess_run` acts on running/stalled), which would "recover" it → `enqueue_advance` → but `driver_for` only returns `ClaudeDriver`, so there's no fallback drive step; `recover_run`'s `heartbeat_at=now()` would just mask idleness. *(P5.3 review, forward-looking.)* | coarse fallbacks have no headless drive step; the watchdog assumes a drivable run | P5.3b must exclude Fallback-backed runs from the watchdog's recover/crash ladder (e.g. a run→driver/capability check in `list_recoverable_runs` or `assess_run`), OR use a distinct coarse-idle status that the watchdog ignores |

## How to use this

- Rejecting an idea? Add a **Discarded** row with the reason — future-you (and the
  next assistant) won't waste a cycle re-proposing it.
- Parking an idea? Add a **Deferred** row with an explicit *revisit-when* trigger.
- Promoting a deferred idea to active work? Move it into
  [open-issues.md](README.md) and note the date here.
