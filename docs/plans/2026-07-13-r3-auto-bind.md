# R3 — project→Dōjō auto-bind (infer + confirm)

> Jerry-approved 2026-07-13. `org_slugs text[]` DDL approved. Bind = INFER + CONFIRM.
> `kind` (employer|client|community|personal) already encodes company/client; the only
> new field is `org_slugs`. Precedence reuses client_precedence_route's order.

## Data path (verified)
- Git-remote owner: `sensei.folders.remote_urls` jsonb `[{name,url}]` → owner via the
  positional segment before the repo. Reuse/extend `repo_tokens_from_remote` (pg_store.rs:210).
- Membership org coverage: NEW `org_slugs text[]` on `dojo.memberships` (service source of truth)
  + `sensei.dojo_memberships` (daemon-local mirror, so infer runs locally).
- Binding target: `sensei.projects.dojo_id` → membership id (existing).

## Commit 1 — data + org-tagging backend
DDL (full-file, additive `org_slugs text[] not null default '{}'`):
- [ ] database/ddl/table/dojo/memberships.ddl
- [ ] database/ddl/table/sensei/dojo_memberships.ddl
pg_store:
- [ ] NewDojoMembership.org_slugs + DojoMembership.org_slugs
- [ ] create_dojo_membership INSERT ($11), DOJO_SELECT, map_dojo_row tuple (14-wide)
- [ ] set_dojo_membership_orgs(id, &[String]) — org-tagging edit
- [ ] roundtrip test asserts org_slugs
memberships.rs: NewConnection.org_slugs (normalize lc/trim/dedup) + validated + register + ConnectionView.org_slugs
handler dojo.rs: NewMembershipBody.org_slugs passthrough; PUT /api/dojo/memberships/{id}/orgs
Deploy: `DATABASE_URL=…/sensei_test dbd deploy --scope default -c database/design.yaml -s database` (dry-run first). Live sensei DB migrates at next install (v0.3.1 was cut before this DDL — known released-bundle gotcha).

## Commit 2 — inference + suggest/bind endpoints
routing.rs (pure, unit-tested matrix):
- [ ] KIND_PRECEDENCE const (shared with client_precedence_route)
- [ ] BindCandidate{membership_id,kind,org_slugs}; InferredBinding{membership_id,kind,matched_slug}
- [ ] infer_binding(project_owners, candidates) → highest-precedence membership whose org_slugs ∋ any owner (case-insensitive)
pg_store: remote_path_segments extraction (DRY) + remote_owner_slug + project_org_owners(project_id)
orchestration: suggest_binding(pg, project_id) → None if already bound, else infer
API: GET /api/projects/{id}/dojo-suggestion ; POST /api/projects/{id}/dojo-binding {membership_id}

## Commit 3 — frontend (svelte-file-editor agent, rokkit tokens)
- About panel InappBind confirm chip → suggestion + Confirm(bind)/Dismiss
- Connect flow org_slugs input (org-tagging) beside kind picker
- Playwright e2e: injected membership+matching remote → chip → Confirm binds

## Confidentiality note
Inference only SUGGESTS a binding; never auto-commits and never routes/publishes. The
confidentiality gates (client_precedence_route, dereference) are unchanged and downstream.
