[package]
name = "sensei-transcript-formats"
version = "0.9.1"
edition = "2024"
description = "Readers for the on-disk transcript formats sensei ingests. Shared so there is exactly one implementation per format."

[dependencies]
serde_json = "1"
