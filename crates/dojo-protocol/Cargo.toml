[package]
name = "dojo-protocol"
version = "0.2.29"
edition = "2024"
description = "Shared wire types + content signing for sensei Dōjō artifact federation"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
