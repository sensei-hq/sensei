# homebrew-tap

Homebrew tap for sensei — AI development intelligence.

## Install

```sh
brew tap sensei-hq/tap
brew install sensei-hq/tap/sensei
```

This installs two binaries: `sensei` (CLI) and `senseid` (daemon).
