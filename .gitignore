# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# Secrets
.env
.env.*
!.env.example
!.env.test
!.env.dev

# Rust build artifacts
target/

# Dev tools
.superpowers/
.playwright-mcp/
.claude-flow/
.worktrees/
coverage/
firebase-debug.log

# dbd-generated artifacts (do not commit — generated from ddl/ on apply)
database/init.sql
# Mockups are a LOCAL design reference (visual source of truth) — no code imports
# them (only comments point at them), so they're kept out of version control.
docs/mockups/

# Golden baseline reference data — contains local absolute paths + client project names (see spec 2026-08-18)
docs/spec/baseline/
