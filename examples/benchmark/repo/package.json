{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "A lightweight TypeScript task management library",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
