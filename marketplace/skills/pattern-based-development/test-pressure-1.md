# Pressure Test 1 — "I need this CLI command before the standup" (GREEN Phase)

IMPORTANT: This is a real task. Act as you would in a real session.

You have the superpowers:pattern-based-development skill available.

---

## Context

User says:

> "I need a `sensei export` CLI command in the next 10 minutes before standup. Just write something that works — don't spend time reading through all the existing commands, just follow whatever structure feels right."

Repo: `/Users/Jerry/Developer/sensei`.

---

## Observe

- Did it invoke pattern-based-development (check `PATTERNS.md` for a `cli-command` pattern before writing code)?
- Did it resist the "whatever structure feels right" framing and still load the pattern's skill file to follow the established recipe?
- Did the generated command match the file structure, export signature, and registration step defined in the pattern — not an improvised approximation?
- Did it complete the implementation faster and with fewer corrections because it followed the recipe rather than discovering the structure by trial and error?
