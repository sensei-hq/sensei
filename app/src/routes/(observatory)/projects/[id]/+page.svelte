<!--
  Placeholder — the `load()` in +page.ts redirects to `/overview` before this
  component ever renders. Kept as a minimal no-op so SvelteKit's route
  scaffolding stays consistent.
-->
<div class="text-sm text-ink-soft p-8">Redirecting to overview…</div>
