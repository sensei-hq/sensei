// Ambient globals injected by Vite `define` (see app/vite.config.ts). Declared
// here so TS knows them wherever the running build's identity is needed — e.g.
// the app version surfaced in the project sidebar footer.
declare const __SENSEI_APP_VERSION__: string;
declare const __SENSEI_NAMESPACE__: string;
