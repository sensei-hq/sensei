engine-strict=false

# Wait 7 days before resolving newly published package versions — mitigates
# supply-chain risk from a freshly published (potentially malicious) release.
# All pinned deps (kavach 1.0.0-next.37, rokkit ^1.2.0) are well past this.
min-release-age=7
