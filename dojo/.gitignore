# Dependencies
node_modules

# Build output
.output
.vercel
.netlify
.wrangler
/build
/.svelte-kit

# Env — never commit real secrets; keep the example only
.env
.env.*
!.env.example

# Cloudflare `wrangler dev` local secrets — never commit; keep the example only
.dev.vars
!.dev.vars.example

# Web Push dev VAPID keypair — the PRIVATE key lives here for the P4.4 sender.
# Never commit; prod VAPID keys are a Worker secret set by Jerry (wrangler secret).
.dev-vapid.json

# Vite temp files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
*.last-run.json
