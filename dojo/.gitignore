# Dependencies
node_modules

# Build output
.output
.vercel
.netlify
.wrangler
/build
/.svelte-kit

# Env — never commit real secrets; keep the example only
.env
.env.*
!.env.example

# Cloudflare `wrangler dev` local secrets — never commit; keep the example only
.dev.vars
!.dev.vars.example

# Vite temp files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
*.last-run.json
